# ==============================================================
# Source de donn�es
# ==============================================================
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/AtypikhouseBDD?useUnicode==true&characterEncoding=UTF-8&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
spring.datasource.username= root
spring.datasource.password= password7
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# ==============================================================
# Maintenir la connexion en état si elle est inactive pendant une longue p�riode (n�cessaire dans la production)
# ==============================================================
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1
#server.port=8284
# ==============================================================
# Afficher les instructions SQL Hibernate/Jpa    
# ==============================================================
spring.jpa.show-sql = true
# ==============================================================
# = Hibernate ddl auto (create, create-drop, update)
# ==============================================================
spring.jpa.hibernate.ddl-auto = update
# ==============================================================
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
# ==============================================================
spring.security.user.name=user
spring.security.user.password=pass
spring.security.user.roles=USER
# ==============================================================
# = Spring Security / Queries pour AuthenticationManagerBuilder
# ==============================================================
jwt.secret=atypikhouse
spring.queries.users-query=select username,password ,1 from utilisateur where username=?
spring.queries.roles-query=select username, role  from utilisateur where username=?
spring.datasource.initialization-mode=always
